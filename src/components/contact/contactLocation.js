import { elementData } from "../global/elementData";

const contactLocation = elementData(
    "contact-location",
    "iframe"
);

contactLocation.source = "";
contactLocation.frameborder = 0;

export {
    contactLocation
};