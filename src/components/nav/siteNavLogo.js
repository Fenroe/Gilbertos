import { elementData } from "../global/elementData";

const siteNavLogo = elementData(
    "site-nav-logo",
    "a",
    []
);

siteNavLogo.text = "Giliberto's";
siteNavLogo.href = "#";

export {
    siteNavLogo
};